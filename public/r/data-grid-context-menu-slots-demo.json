{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "data-grid-context-menu-slots-demo",
  "title": "Data Grid Context Menu Slots Demo",
  "author": "gridcn",
  "description": "Custom context menu items via renderCellMenuItems and renderHeaderMenuItems, replacing the built-in item sets.",
  "registryDependencies": [
    "DammersCode/gridcn/data-grid",
    "DammersCode/gridcn/data-grid-context-menu"
  ],
  "files": [
    {
      "path": "registry/default/examples/data-grid-context-menu-slots-demo.tsx",
      "content": "\"use client\";\n\nimport { useMemo, type ReactNode } from \"react\";\nimport { ArrowUp, Copy, Plus } from \"lucide-react\";\nimport {\n  DataGridProvider,\n  DataGridRoot,\n  DataGridHeader,\n  DataGridBody,\n  defineColumns,\n  useDataGridActions,\n} from \"@/registry/default/components/data-grid/data-grid\";\nimport { DataGridContextMenu, DataGridHeaderDropdown } from \"@/registry/default/components/data-grid-context-menu/data-grid-context-menu\";\nimport { ContextMenuItem } from \"@/components/ui/context-menu\";\nimport { generateDemoRows, type DemoRow } from \"./demo-data\";\n\nconst columns = defineColumns<DemoRow>()([\n  { id: \"name\", header: \"Name\", accessorKey: \"name\", type: \"text\", width: 100, flex: 1 },\n  { id: \"email\", header: \"Email\", accessorKey: \"email\", type: \"text\", width: 130, flex: 2 },\n  { id: \"score\", header: \"Score\", accessorKey: \"score\", type: \"number\", width: 70, flex: 1 },\n] as const);\n\nfunction createRow(): DemoRow {\n  return { id: `new-${Date.now()}`, name: \"New row\", email: \"\", age: 18, active: false, role: \"User\", joined: \"\", score: 0 };\n}\n\nfunction duplicateRow(row: DemoRow): DemoRow {\n  return { ...row, id: `${row.id}-copy-${Date.now()}` };\n}\n\nfunction DuplicateRowItem({ row }: { row: number }) {\n  const actions = useDataGridActions();\n  return (\n    <ContextMenuItem onClick={() => actions.duplicateRows([row])}>\n      <Copy className=\"size-4 text-muted-foreground\" />\n      Duplicate row\n    </ContextMenuItem>\n  );\n}\n\nfunction SortAscendingItem({ columnId }: { columnId: string }) {\n  const actions = useDataGridActions();\n  return (\n    <ContextMenuItem onClick={() => actions.setSorts([{ columnId, direction: \"asc\" }])}>\n      <ArrowUp className=\"size-4 text-muted-foreground\" />\n      Sort ascending\n    </ContextMenuItem>\n  );\n}\n\nfunction InsertRowItem({ row }: { row: number }) {\n  const actions = useDataGridActions();\n  return (\n    <ContextMenuItem onClick={() => actions.insertRow(row, \"below\")}>\n      <Plus className=\"size-4 text-muted-foreground\" />\n      Insert row below\n    </ContextMenuItem>\n  );\n}\n\nexport default function DataGridContextMenuSlotsDemo(): ReactNode {\n  const rows = useMemo(() => generateDemoRows(10), []);\n\n  return (\n    <div className=\"w-full flex flex-col gap-3\">\n      <p className=\"text-sm text-muted-foreground\">\n        Right-click a cell for a two-item row menu. Right-click a header for a one-item column menu.\n      </p>\n      <div className=\"h-[360px] overflow-hidden rounded-md border border-border\">\n        <DataGridProvider\n          defaultData={rows}\n          columns={columns}\n          getRowId={(row) => row.id}\n          createRow={createRow}\n          duplicateRow={duplicateRow}\n        >\n          <DataGridContextMenu\n            className=\"flex h-full flex-col\"\n            renderCellMenuItems={({ row, canInsertRow, canDuplicateRow }) => (\n              <>\n                {canInsertRow && <InsertRowItem row={row} />}\n                {canDuplicateRow && <DuplicateRowItem row={row} />}\n              </>\n            )}\n            renderHeaderMenuItems={({ columnId }) => <SortAscendingItem columnId={columnId} />}\n          >\n            <DataGridRoot\n              className=\"h-full rounded-none border-none\"\n              renderHeaderMenu={(ctx) => <DataGridHeaderDropdown {...ctx} />}\n            >\n              <DataGridHeader />\n              <DataGridBody />\n            </DataGridRoot>\n          </DataGridContextMenu>\n        </DataGridProvider>\n      </div>\n    </div>\n  );\n}\n",
      "type": "registry:example",
      "target": "components/data-grid-context-menu-slots-demo.tsx"
    },
    {
      "path": "registry/default/examples/demo-data.ts",
      "content": "/** Deterministic LCG so SSR/CSR markup match without Math.random(); shared across example rows. */\r\nfunction createSeededLcg(seed: number) {\r\n  let state = seed;\r\n  return () => {\r\n    state = (state * 1103515245 + 12345) & 0x7fffffff;\r\n    return state / 0x7fffffff;\r\n  };\r\n}\r\n\r\nexport type DemoRow = {\r\n  id: string;\r\n  name: string;\r\n  email: string;\r\n  age: number;\r\n  active: boolean;\r\n  role: string;\r\n  joined: string;\r\n  score: number;\r\n};\r\n\r\nconst ROLES = [\"Admin\", \"User\", \"Editor\", \"Viewer\", \"Manager\"] as const;\r\nconst ADJECTIVES = [\"Eager\", \"Quick\", \"Silent\", \"Bold\", \"Swift\"] as const;\r\nconst NOUNS = [\"Eagle\", \"Lion\", \"Tiger\", \"Bear\", \"Wolf\"] as const;\r\n\r\n/** Generates `count` deterministic demo rows for example previews (seed 42, stable across renders). */\r\nexport function generateDemoRows(count: number, seed = 42): DemoRow[] {\r\n  const next = createSeededLcg(seed);\r\n  const rows: DemoRow[] = [];\r\n  for (let i = 0; i < count; i++) {\r\n    rows.push({\r\n      id: `row-${i}`,\r\n      name: `${ADJECTIVES[Math.floor(next() * ADJECTIVES.length)]} ${NOUNS[Math.floor(next() * NOUNS.length)]}`,\r\n      email: `user${i}@example.com`,\r\n      age: Math.floor(next() * 50) + 18,\r\n      active: next() > 0.5,\r\n      role: ROLES[Math.floor(next() * ROLES.length)]!, // ROLES is a fixed non-empty literal array\r\n      joined: `${2020 + Math.floor(next() * 5)}-${String(Math.floor(next() * 12) + 1).padStart(2, \"0\")}-${String(Math.floor(next() * 28) + 1).padStart(2, \"0\")}`,\r\n      score: Math.floor(next() * 100),\r\n    });\r\n  }\r\n  return rows;\r\n}\r\n",
      "type": "registry:example",
      "target": "components/demo-data.ts"
    }
  ],
  "categories": [
    "data"
  ],
  "type": "registry:example"
}
