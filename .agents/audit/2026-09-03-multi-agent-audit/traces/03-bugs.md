# Trace — bugs audit agent

- Task id: `ses_f9992c3ebffe0mj5PEl58nhMZK` (opencode task tool, type `general`)
- Mandate: read-only, research only; bugs & correctness dimension
- Input prompt: carried PLAN.md (§2/§4.3/§5/§11a), glide-behavior-spec, diceui-editing-spec,
  the full 08-02 audit (verified + 65-row tail), the not-supported register, the
  6-priority re-verify list (incl. the two gating compare-wiring follow-ups), the 10-item
  fresh-pass hunt list, and the test-gap pass.
- Returned: raw report below, unedited. Orchestrator vetted N1 (use-data-grid-history.ts:88,
  interaction/history.ts:52-83) and the prior-priority fixes against source — all confirmed.

---

All verification complete. Writing the final report.

## Bugs audit — 2026-09-03

Re-verification of `docs/agent-work/2026-08-02-optimization-audit.md` findings against HEAD `b620e4a`, plus a fresh adversarial pass (index-space mixing, editing lifecycle, keyboard map, clipboard, fill, selection, history, data integrity, a11y, cross-addon interaction) and a test-gap pass. Research-only; no modifications.

### Prior-audit bug findings: verdicts

**Six named priorities**

| # | Prior finding | Verdict | Evidence |
|---|---|---|---|
| 1 | `streamGeneration` token bump on sync `updateCells` path mis-supersedes held async batches | **FIXED** | `registry/default/blocks/data-grid/store/create-store.ts:676-687` — counter bump now inside `if (validated instanceof Promise)` only; `forgetDeferredRows`/`resetDeferredRows` (create-store.ts:126,130) also bump, documented as intentional supersede. Regression test: `store/update-cells.test.tsx:334` ("a newer batch supersedes a held one — the older resolution never lands") + `:362` (workplan #83 sync-schema no-discard). |
| 2 | Cell-type `compare` not wired into sort (typed columns fall back to default compare) | **FIXED** | `store/compute.ts` `textAccessorFor` (~166-225) wires `accessor.compare` for typed columns (empty-last honored via `isEmptyCell`). |
| 2a | Follow-up (a): empty-last rule not applied to custom comparators in full rebuild | **FIXED** | `sort-filter/build-view-index.ts:91-105` — custom comparator wrapped with `empties` map applying empty-last; `isEmptyCell` at :22-24 honors `accessor.isEmpty`. |
| 2b | Follow-up (b): custom-comparator bail defeated the incremental view-index path | **FIXED** | `sort-filter/incremental-view-index.ts:22-46,52` — `"custom-comparator"` retained in the bail type but no longer fires: the resolved `accessor.compare` rides the incremental path through `compareOnColumn` with the same empty-last rule. |
| 3 | `resolveContextMenuTarget` mapped view row via `ariaRowindex-2`, wrong for pinned rows / marker column | **FIXED** | `registry/default/blocks/data-grid-context-menu/resolve-context-menu-target.ts:25-30` — `resolveViewRow` prefers the `data-grid-row-index` stamp (set at `registry/default/blocks/data-grid/row.tsx:86`, attribute in `data-attributes.ts:20`), `ariaRow-2` only as legacy fallback; pinned-row cells excluded via `:not(gridAttrSelector("pinnedRow"))` selector (~:51). |
| 4 | `pruneCellErrors` missed error keys with colons (compound ids) | **FIXED** | `store/compute.ts:57-62` — `cellErrorKeyIsLive` tries every colon position; `cellErrorKey` exported (compute.ts:47, `store/index.ts:23`). |
| 5 | `pagination-footer.tsx` imported labels from a deep path bypassing the barrel boundary | **FIXED** | `registry/default/blocks/data-grid-pagination/pagination-footer.tsx:8` — imports from `@/registry/default/blocks/data-grid/data-grid`. |
| 6 | No gate against registry payload alias drift (`@/registry/...` leaking into built `public/r/*.json`) | **FIXED** | `scripts/fix-registry-imports.mjs` — generic regex + fail-loud `process.exit(1)` gate (~:26-27, 62-85); `scripts/verify-payload-content.mjs` runs pre-rebuild in CI (`.github/workflows/ci.yml:53`; `package.json:12-13` `registry:build`/`registry:verify`). Scan of `public/r/*.json` confirms zero surviving deep aliases. |

**Tail items (from the audit's 65-finding tail)**

| Prior finding | Verdict | Evidence |
|---|---|---|
| `useDataGridRowHasError` prefix-matched column ids (`"id"` matched `"idx"`) | **FIXED** | `store/hooks.ts:204-212` — exact `cellErrorKey` probe. |
| `useDataGridAllRowsSelectedState` O(n) scan per marker render | **FIXED** | `store/hooks.ts:634-669` — O(rangeStack) over the selection's row ranges. |
| No rowId-native presence adapter (consumers re-implemented indexOf) | **FIXED** | `store/hooks.ts:377-392` — `useDataGridRowIdToViewRow`. |
| Marker-header called the selection hook unconditionally (cost paid per marker row) | **Moot** | Hook is now O(rangeStack) (above); unconditional call no longer a cost. |
| Context-menu shortcut hints showed default bindings, ignoring a consumer `keymap` | **FIXED** | `data-grid-context-menu/cell-menu-content.tsx:46` — `useDataGridKeymap()` (effective map). |
| `downloadBlob` revoked the object URL immediately (download race) | **FIXED** | `data-grid-io/export-grid.ts:97` — deferred `setTimeout` revoke (10 s). |
| Excel import silently dropped sheets beyond the first | **FIXED** | `data-grid-io/parse-import-file.ts:47` — `sheetNames` surfaced; `import-dialog.tsx:73,201` — `multiSheetNotice`; regression test `parse-import-file.test.ts:98+`. |
| `useDataGridFill` allocated a fresh tracker object per render (defeated plugin memo) | **FIXED** | `data-grid-fill/use-data-grid-fill.tsx:63-68` — `useMemo`; `test/fill-tracker-identity.browser.test.tsx` pins it. |
| RTL browser test self-skipped when it couldn't find its own grid (false green) | **FIXED** | `test/rtl.browser.test.tsx:201-202,327` — hard assertions via `gridAttrSelector`. |
| `ColumnDefOf` / `ClipboardProcessCtx` types not re-exported from the public entry | **FIXED** | `data-grid.tsx:137`. |
| `DataGrid` wrapper dropped `duplicateRow` / `cellTypes` / `labels` props | **FIXED** | `data-grid.tsx:267,299,307,435,448,450`. |
| `isDev` not on the barrel (lazy-guard deep-imported) | **FIXED** | `data-grid.tsx:197`; `data-grid-lazy/lazy-guard.tsx:4-9` imports from the barrel. |
| No lint boundary forcing barrel-only imports in consumer code | **FIXED** | `eslint.config.mjs:104-112` — `no-restricted-imports` deep-path deny. |
| `registry-smoke.ps1` hardcoded the smoke item list (drifted from registry.json) | **FIXED** | `scripts/registry-smoke.ps1:107-109` — derives items from registry.json (+ 2 deliberate cross-addon demos). |
| `getRowId` arity differed between core and history add-on | **FIXED** | `data-grid-history/use-data-grid-state.ts:9` — unified; `get-row-id-arity.type-test.ts` pins it. |
| `insertRow` / `duplicateRows` left stale `cellErrors` entries for moved/removed rows | **FIXED** | `store/create-store.ts:773,792,821` — `pruneCellErrors` on insert/duplicate/delete. |
| `applyCellUpdates` / `deleteSelection` didn't reconcile deferred view state | **FIXED** | `store/create-store.ts:554,568,625,660` — `reconcileAfterWrite` on all four write paths. |
| `serializeCopyScope` re-resolved the scope per cell + unbounded copy size | **FIXED** | `clipboard/use-grid-clipboard.ts` — hoisted resolution + `MAX_COPY_CELLS = 200_000`. |
| TSV parse char-by-char `+=` allocation at scale | **FIXED** | `clipboard/parse-clipboard.ts:55-63` — `start`/`slice` flush (run-based); `clipboard/parse-clipboard-perf.test.ts` pins the regression. |
| `insertRowBelow` / `duplicateRow` had actions but no default keybindings | **FIXED** | `keyboard/default-keymap.ts:52-53` — `mod+shift+f` / `mod+shift+x`. |
| Paste batch could commit twice / lose position across reorder | **FIXED** | `clipboard/use-grid-clipboard.ts:268,277` — one `applyCellUpdates("paste")`, held path re-resolves via rowId; supersede/drop-on-sort-change/drop-on-delete covered in `clipboard/use-data-grid-clipboard.test.tsx:120-220`. |
| Scroll snapshot read geometry twice per tick (forced reflow after the var write) | **FIXED** | `windowing/use-scroll-snapshot.ts:174-177` — one hoisted `readGeometry` before the `writeScrollVars` write (the audit's confirmed medium). |
| Streaming heap test no-op'd green when `performance.memory` was unavailable | **FIXED** | `test/streaming.browser.test.tsx:227-229` — loud `toBeGreaterThan(0)` fail. |
| URL pagination page-size-change semantics ambiguous vs client hook | **Verified as documented** | Client hook keeps first-visible row (`data-grid-pagination/use-data-grid-pagination.ts:119-124`); URL hook resets page — prescribed by `docs/agent-work/plans/2026-07-17-qa-workplan.md:1042` ("pageSize-change resets page"); both behaviors tested (`use-data-grid-url-pagination.test.tsx:94-117`). |
| 6 store hooks exported from the store barrel but absent from the public entry | **STILL OPEN (dx, low)** | `getFocusCell`, `useDataGridCellTypes`, `useDataGridFillHandlers`, `useDataGridActiveColumn`, `useDataGridOverlayPlugins`, `useDataGridRowBands` — reachable via `store/index.ts:24-39` but not re-exported by `data-grid.tsx`. Mostly internal provider seams (fill handlers, overlay plugins, row bands are workplan #48 cut-#3 seams); a consumer who imports the store barrel gets them. Surface inconsistency only. |

### New findings

| # | Kind | Finding | Evidence | Failure scenario | Spec | Effort | Confidence |
|---|---|---|---|---|---|---|---|
| N1 | perf (medium) | Undo/redo row lookup is O(n²) per op at scale. `findRowId` unconditionally runs an O(n) `dataRef.current.indexOf(row)` for every row examined; `applyChange` calls it once per row inside `findIndex` for each update/delete op. One op whose row sits near the end (or whose row has vanished → full scan) costs ~n×n element comparisons; a k-op batch on n rows approaches k·n². The unconditional `indexOf` happens even for consumers with a stable-id `getRowId` that ignores its index arg. | `registry/default/blocks/data-grid-history/use-data-grid-history.ts:88` (`findRowId`); `registry/default/blocks/data-grid/interaction/history.ts:57,62,68,70` (`data.slice()`, `findIndex` per update/delete op, `slice+concat` per delete) | Undo/redo of a multi-row gesture (e.g. undo a 1,000-row range edit or import) on a ~100k-row grid → multi-second main-thread freeze. Small single-cell undos near the top of the data match early and stay cheap — which is why it's invisible in normal use. | PLAN.md §5 (one batch per gesture; the undo/redo re-apply should not be an order of magnitude costlier than the write it reverses) | M | Mechanism **CONFIRMED** (code inspection); magnitude **PLAUSIBLE** (no benchmark run — research-only scope) |
| N2 | spec gap (low) | `Alt+Arrow` — move active cell while retaining selection — is specified but has no action and no binding. | Spec: `research/glide-behavior-spec.md:41`; absent from `GridAction` (`registry/default/blocks/data-grid/types.ts:396-410`) and `DEFAULT_KEYMAP` (`keyboard/default-keymap.ts`). The matcher already supports the `alt` modifier (`keyboard/match-keymap.ts:47`, tested in `keyboard/index.test.ts:246-269`). | Excel users hit Alt+Arrow and get plain move (selection collapses) instead of move-with-retained-selection. | glide spec §2 | S | CONFIRMED |
| N3 | spec gap (low) | `primary+Enter` — scroll active cell into view without moving — is specified but unbound. The capability exists and is registered but has no keymap action. | Spec: `research/glide-behavior-spec.md:46`; capability: `interaction/use-grid-interaction.ts:181,906`, registered at `root.tsx:227` via `_registerScrollToCell`. | Off-screen active cell (e.g. after a programmatic selection from a toolbar) can't be scrolled into view via the spec'd keystroke; only the API exists. | glide spec §2 | S | CONFIRMED |
| N4 | spec gap (low) | "Click outside grid → clear selection, fire `onSelectionCleared`" is only half-implemented: (a) the clear fires only for a pointerdown whose target is the grid root element itself (its own gutter/empty strip) — a press anywhere else on the page keeps the selection; (b) `onSelectionCleared` does not exist anywhere in the registry. | `interaction/use-grid-interaction.ts:876-887` (`onRootPointerDown` bails on `event.target !== event.currentTarget`); grep of `registry/` for `onSelectionCleared`/`SelectionCleared` → zero hits. `onSelectionChange` deliberately does not fire on clear (`test/store.test.tsx:2129-2137`). | Consumer relying on the spec'd callback (e.g. to reset an "N cells selected" toolbar chip) has no hook; a page-area click leaves stale selection visible. | glide spec §3 (`research/glide-behavior-spec.md:108`) | S | CONFIRMED |
| N5 | edge-case (low) | Lazy rows: a `fetchRows` that resolves short marks the whole requested range as loaded, so trailing holes are never re-fetched and `pendingCount` undercounts. | `data-grid-lazy/use-data-grid-lazy-rows.ts:123-133` (`handleFulfilled` unconditionally `mergeRanges` the full range, then writes only `fetched.length` rows); `pendingCount` derived from the same bookkeeping at :185. | Dataset shrinks between window-report and fetch resolution (or a buggy/contract-violating `fetchRows` returns fewer rows) → skeleton rows persist permanently in that range with no retry; `hasHoles`/`pendingCount` say the data is complete. Contract-level (doc says `fetchRows` "fetches rows `[start, end)`"), so low. | lazy-loading design doc ("refetch on next visibility" applies to failures, not short successes) | S | PLAUSIBLE |
| N6 | spec ambiguity (note) | `mod+shift+Arrow` extends to the *data boundary*, not the grid edge. Spec §2 line 52 says "grow to grid edge", but line 43's own note mandates Excel-style data-boundary jumps for `mod+Arrow` and gridcn applies the same `jumpToDataBoundary` to the shift variant — internally consistent, just not matching line 52's wording. | `keyboard/default-keymap.ts:29-32`; `interaction/use-grid-interaction.ts:302` (`JumpAction` incl. `extendJump*`); spec `research/glide-behavior-spec.md:43,52`. | None functional; a spec-doc correction at most. | glide spec §2 | S (doc) | CONFIRMED |

Fresh pass otherwise clean: fill pipeline (Alt→forceCopy at release `use-fill-handle.ts:270,293`, `onFillPattern` veto :219-221, single `applyCellUpdates` batch :230/233, supersede guard :225-229, Escape-cancel :274-278), fill-target math (`compute-fill-target.ts:34-81`), TSV/HTML clipboard round-trip (`data-gridcn-raw` raw-attr fidelity; `fieldPending` handles `""` empties; `parse-clipboard.ts:124-126` trailing-newline semantics match split-and-drop), import build (chunked + cancel + invalid-value clearing, `build-imported-rows.ts`), URL pagination (clamps, omit-at-default, replace-history, prefixing — all tested), pagination math (first-visible-row preserved on size change, `use-data-grid-pagination.ts:119-124`), lazy dedup/abort bookkeeping (synchronous `inFlightRef` consult, abort-on-unmount/totalCount-change, sync-throw guard), selection lib (two-stage Ctrl+A, Excel-contract grow/shrink, line-toggle), commit immutability, a11y index-space (marker column documented out-of-data-space at `header.tsx:19-29`; `aria-rowindex`/`aria-rowcount` math consistent), IME `isComposing` edit guard (`use-grid-interaction.ts:535`).

### Test gaps

| Gap | Suggested location | Linked finding | Priority |
|---|---|---|---|
| No guard on undo/redo cost at scale: `history.test.ts` uses ≤5-row arrays; there is no 100k-row arm for `applyChange` the way `streaming.browser.test.tsx` has for `updateCells` (identity/median-per-op assertions). The O(n²) `findRowId` would regress silently. | `registry/default/blocks/data-grid-history/` — a browser perf arm (100k rows, undo a 500-op batch, assert median per-op < bar) + a unit test asserting `findRowId` is called O(matched-position), not O(n), per op | N1 | High |
| No tests for the three missing spec'd behaviors (Alt+Arrow retain-selection move; primary+Enter scroll-into-view; page-area click clearing selection + `onSelectionCleared` callback) — nothing exists to test today; add when implemented. | `keyboard/index.test.tsx` (matching), `interaction/use-grid-interaction.test.tsx` / a browser test (behavior + callback) | N2, N3, N4 | Medium |
| No test for `useDataGridLazyRows` short-fetch resolution (fetch resolves with fewer rows than requested): holes stay, range marked loaded, `pendingCount` undercounts, no retry. | `registry/default/blocks/data-grid-lazy/use-data-grid-lazy-rows.test.ts` — stub `fetchRows` to resolve short and assert retry-on-next-visibility (or assert documented contract) | N5 | Low |
| Covered — no gap (verified this pass): stream-generation supersede (`update-cells.test.tsx:334,362`); paste supersede/snapshot-drop (`use-data-grid-clipboard.test.tsx:120-220`, `async-validation.browser.test.tsx:93`); TSV quoted-empty round-trip + diff-vs-reference (`clipboard/index.test.ts:169-174`, `parse-clipboard-diff.test.ts`); TSV parse perf pin (`parse-clipboard-perf.test.ts`); URL pagination read/write/clamp/identity (`use-data-grid-url-pagination.test.tsx`); streaming heap no-op hard-fail (`streaming.browser.test.tsx:227-229`); fill identity (`fill-tracker-identity.browser.test.tsx`); fill pipeline (unit + browser, `data-grid-fill/fill.test.ts`, `use-fill-handle.test.tsx`, `fill.browser.test.tsx`). | — | — | — |
